import http from 'http';import fs from 'fs';import path from 'path';import{fileURLToPath}from 'url';const __dirname=path.dirname(fileURLToPath(import.meta.url));function loadEnv(){const e=path.join(__dirname,'../.env.local');if(!fs.existsSync(e))return;for(const l of fs.readFileSync(e,'utf-8').split('\n')){const[k,...r]=l.split('=');if(k&&r.length)process.env[k.trim()]=r.join('=').trim()}}loadEnv();http.createServer(async(req,res)=>{res.setHeader('Access-Control-Allow-Origin','http://localhost:5173');res.setHeader('Access-Control-Allow-Methods','POST,OPTIONS');res.setHeader('Access-Control-Allow-Headers','Content-Type');if(req.method==='OPTIONS'){res.writeHead(204);return res.end()}let b='';req.on('data',c=>b+=c);req.on('end',async()=>{try{const{messages,system}=JSON.parse(b);const k=process.env.ANTHROPIC_API_KEY;if(!k)throw new Error('API Key nao encontrada');const u=await fetch('https://api.anthropic.com/v1/messages',{method:'POST',headers:{'Content-Type':'application/json','x-api-key':k,'anthropic-version':'2023-06-01'},body:JSON.stringify({model:'claude-sonnet-4-6',max_tokens:2000,system,messages})});const t=await u.text();res.writeHead(u.status,{'Content-Type':'application/json'});res.end(t)}catch(e){res.writeHead(500,{'Content-Type':'application/json'});res.end(JSON.stringify({error:e.message}))}})}).listen(3001,()=>{console.log('Servidor: http://localhost:3001');console.log('API Key: '+(process.env.ANTHROPIC_API_KEY?'encontrada':'NAO encontrada'))})